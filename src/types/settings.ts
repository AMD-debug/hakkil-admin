export interface SiteSettings {
  phone?: string; // affichage, ex. "+224 6XX XX XX XX"
  whatsapp?: string; // numéro international sans symboles, ex. "224600000000"
  email?: string;
  address?: string;
}
